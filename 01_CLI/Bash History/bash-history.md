# Bash History
- Commands persist to `~/.bash_history` when the shell exits; view tail with `tail ~/.bash_history`.
- History powers Up/Down navigation and `Ctrl+R` search.
- Avoid secrets in commands (e.g. `--password=...`); edit the file after the fact if you need to remove sensitive lines.
